[project]
name = "subtitle_tool"
version = "0.1.0"

requires-python = ">= 3.12"
dependencies = ["mashumaro", "toml"]

[project.optional-dependencies]
dev = ["pytest", "mypy", "types-toml"]

[project.scripts]
st = "subtitle_tool.cli:entry_point"

[tool.mypy]
mypy_path = "$MYPY_CONFIG_FILE_DIR/src:$MYPY_CONFIG_FILE_DIR/tests"
files = ["src", "tests"]
strict = true
enable_error_code = "unreachable, redundant-expr, possibly-undefined, truthy-bool, truthy-iterable, unused-awaitable, explicit-override, mutable-override"

[tool.pytest.ini_options]
testpaths = "tests"

[tool.black]
skip-magic-trailing-comma = true

[tool.isort]
profile = "black"
force_single_line = true
